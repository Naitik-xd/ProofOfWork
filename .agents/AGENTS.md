# Project-Specific Agent Rules

- **PTG Command**: Whenever the user says "PTG", automatically run the commands to add all files, commit them with a generic message (or the one provided), and push to the `main` branch of the `https://github.com/Naitik-xd/ProofOfWork.git` repository.
